{
  "buildCommand": "cd ../.. && yarn workspace @meetup/shared build && yarn workspace @meetup/mock-api build",
  "installCommand": "cd ../.. && yarn install",
  "outputDirectory": "public",
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index" }
  ]
}
